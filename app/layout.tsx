import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

export const metadata: Metadata = {
  title: 'Voice Cart - AI Voice Agents for E-commerce',
  description: 'Transform your e-commerce with Voice Cart AI - intelligent voice agents that boost sales and enhance customer experience across Shopify, WooCommerce, BigCommerce, and Odoo.',
  generator: 'Voice Cart',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
